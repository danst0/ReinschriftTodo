app-id: me.dumke.ReinschriftTodo
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: reinschrift_todo
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --filesystem=home
  - --share=network
build-options:
  append-path: /usr/lib/sdk/rust-stable/bin
modules:
  - name: reinschrift_todo
    buildsystem: simple
    build-commands:
      - cargo build --release --offline
      - install -D target/release/reinschrift_todo /app/bin/reinschrift_todo
      - install -D reinschrift_todo.desktop /app/share/applications/me.dumke.ReinschriftTodo.desktop
      - install -D icon/todos.png /app/share/icons/hicolor/512x512/apps/me.dumke.ReinschriftTodo.png
    sources:
      - type: dir
        path: .
